#!/usr/bin/env ruby
begin
  require "notify-push"
rescue LoadError
  require "rubygems"
  require "notify-push"
end

if ARGV[0] == "--receiver"
  exit NotifyPush::Receiver.start
end

NotifyPush.start_sender(ARGV)
